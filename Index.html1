<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>ASNA Needle & Co. — Handcrafted Textile Art</title>
<meta name="description" content="ASNA Needle & Co. is a design-led studio creating handmade textile pieces.">
<meta property="og:title" content="ASNA Needle & Co. — Handcrafted Textile Art">
<meta property="og:image" content="https://res.cloudinary.com/dg4vourvl/image/upload/v1777148862/Asna_20260425_204824_0000_iah4pc.png">
<meta name="theme-color" content="#ece6d9">
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,500;1,300;1,400&family=Inter:wght@300;400;500&display=swap" rel="stylesheet">
<style>
*{margin:0;padding:0;box-sizing:border-box}
:root{
  --bg:#ece6d9;--primary:#491f11;--secondary:#726054;
  --accent1:#b38c74;--accent2:#946a53;
  --surface1:#e4cdb9;--surface2:#caa892;--neutral:#8b7363;
  --serif:"Cormorant Garamond",serif;--sans:"Inter",sans-serif;
}
html{scroll-behavior:smooth;font-size:16px}
body{background:var(--bg);color:var(--primary);font-family:var(--sans);overflow-x:hidden;cursor:none}

/* THREAD TRAIL CURSOR */
.cursor,.cursor-ring{pointer-events:none;position:fixed;border-radius:50%;z-index:9999;transform:translate(-50%,-50%)}
.cursor{width:8px;height:8px;background:var(--primary);transition:transform .12s,opacity .2s,background .2s}
.cursor-ring{width:36px;height:36px;border:1px solid var(--accent1);z-index:9998;transition:transform .38s cubic-bezier(.25,.46,.45,.94),opacity .2s;opacity:.6}
.cursor.hover{transform:translate(-50%,-50%) scale(2.8);opacity:.35;background:var(--accent2)}
.cursor-ring.hover{transform:translate(-50%,-50%) scale(1.8);opacity:.25}
.cursor-thread{pointer-events:none;position:fixed;border-radius:50%;z-index:9997;transform:translate(-50%,-50%);opacity:0}
@media(hover:none){.cursor,.cursor-ring,.cursor-thread{display:none}body{cursor:auto}}

/* ENTRY */
#entry{position:fixed;inset:0;background:var(--bg);z-index:1000;display:flex;flex-direction:column;align-items:center;justify-content:center;padding:2rem;transition:opacity 1.4s ease,visibility 1.4s ease}
#entry.hidden{opacity:0;visibility:hidden;pointer-events:none}
.entry-label{font-size:.65rem;letter-spacing:.35em;text-transform:uppercase;color:var(--neutral);margin-bottom:2.5rem;opacity:0;animation:fadeUp .9s ease .4s forwards}
.entry-logo{width:min(220px,60vw);opacity:0;animation:fadeIn 1.2s ease .7s forwards;margin-bottom:2.5rem}
.entry-title{font-family:var(--serif);font-size:clamp(1.4rem,4vw,2.4rem);font-weight:300;font-style:italic;color:var(--primary);letter-spacing:.02em;text-align:center;opacity:0;animation:fadeUp 1s ease 1s forwards;margin-bottom:.8rem}
.entry-sub{font-size:.65rem;letter-spacing:.3em;text-transform:uppercase;color:var(--secondary);opacity:0;animation:fadeUp .9s ease 1.3s forwards;margin-bottom:3.5rem;text-align:center}
.entry-line{width:1px;height:60px;background:linear-gradient(to bottom,transparent,var(--accent1));margin:0 auto 2rem;opacity:0;animation:fadeIn 1s ease 1.9s forwards}
.entry-btn{font-size:.6rem;letter-spacing:.3em;text-transform:uppercase;color:var(--primary);border:1px solid var(--accent1);background:none;cursor:none;padding:.9rem 2.4rem;opacity:0;animation:fadeUp .9s ease 1.6s forwards;transition:background .3s,color .3s}
.entry-btn:hover{background:var(--primary);color:var(--bg)}
@media(hover:none){.entry-btn{cursor:pointer}}

/* NAV */
nav{position:fixed;top:0;left:0;right:0;z-index:500;padding:1.4rem 2.4rem;display:flex;align-items:center;justify-content:space-between;background:linear-gradient(to bottom,rgba(236,230,217,.97) 70%,transparent)}
.nav-logo img{height:30px;opacity:.9}
.nav-links{display:flex;gap:2.2rem;list-style:none}
.nav-links a{font-size:.6rem;letter-spacing:.28em;text-transform:uppercase;color:var(--secondary);text-decoration:none;transition:color .3s}
.nav-links a:hover{color:var(--primary)}
.nav-right{display:flex;align-items:center;gap:1.5rem}
.nav-btn{font-size:.6rem;letter-spacing:.28em;text-transform:uppercase;color:var(--secondary);cursor:none;background:none;border:none;position:relative;transition:color .3s}
.nav-btn:hover{color:var(--primary)}
@media(hover:none){.nav-btn{cursor:pointer}}
.badge{position:absolute;top:-6px;right:-12px;background:var(--accent2);color:var(--bg);font-size:.45rem;width:14px;height:14px;border-radius:50%;display:flex;align-items:center;justify-content:center}
.wish-badge{background:var(--primary);display:none}
.wish-badge.has{display:flex}
.hamburger{display:none;background:none;border:none;cursor:pointer;flex-direction:column;gap:5px;padding:4px}
.hamburger span{display:block;width:22px;height:1px;background:var(--primary);transition:transform .3s,opacity .3s}
.hamburger.open span:nth-child(1){transform:translateY(6px) rotate(45deg)}
.hamburger.open span:nth-child(2){opacity:0}
.hamburger.open span:nth-child(3){transform:translateY(-6px) rotate(-45deg)}
.mobile-menu{display:none;position:fixed;top:60px;left:0;right:0;background:var(--bg);border-bottom:1px solid var(--surface2);padding:1.5rem 2.4rem;z-index:499;flex-direction:column;gap:1.2rem}
.mobile-menu a{font-size:.65rem;letter-spacing:.28em;text-transform:uppercase;color:var(--secondary);text-decoration:none}
.mobile-menu.open{display:flex}
@media(max-width:900px){.nav-links{display:none}.hamburger{display:flex}}

/* CRAFT */
#craft{display:grid;grid-template-columns:1fr 1fr;min-height:100vh}
.craft-left{position:relative;overflow:hidden}
.craft-left-img{width:100%;height:100%;object-fit:cover;display:block;filter:saturate(.85)}
.craft-left-overlay{position:absolute;inset:0;background:linear-gradient(to top,rgba(73,31,17,.72) 0%,rgba(73,31,17,.1) 60%)}
.craft-left-text{position:absolute;bottom:0;left:0;right:0;padding:3rem}
.craft-label{font-size:.6rem;letter-spacing:.35em;text-transform:uppercase;color:var(--accent1);margin-bottom:1rem}
.craft-title{font-family:var(--serif);font-size:clamp(2rem,4vw,3.6rem);font-weight:300;line-height:1.1;color:var(--bg)}
.craft-title em{font-style:italic;color:var(--surface2)}
.craft-right{display:flex;flex-direction:column;justify-content:center;padding:5rem 4rem}
.craft-body{font-size:.85rem;line-height:1.9;color:var(--secondary);max-width:360px;margin-bottom:1.4rem}
.craft-note{font-family:var(--serif);font-size:1.1rem;font-style:italic;color:var(--accent1)}
.craft-credit{font-size:.58rem;letter-spacing:.15em;color:var(--neutral);margin-top:2rem;opacity:.7}
@media(max-width:900px){#craft{grid-template-columns:1fr}.craft-left{min-height:60vw}.craft-right{padding:3rem 1.5rem}}

/* PROCESS STRIP */
.process-strip{padding:1.6rem 0;overflow:hidden;border-top:1px solid var(--surface2);border-bottom:1px solid var(--surface2);background:var(--surface1)}
.strip-inner{display:flex;gap:4rem;animation:scrollX 18s linear infinite;white-space:nowrap;align-items:center}
.strip-item{font-family:var(--serif);font-size:1rem;font-style:italic;color:var(--secondary);flex-shrink:0}
.strip-dot{width:4px;height:4px;background:var(--accent1);border-radius:50%;display:inline-block;flex-shrink:0}

/* ROOMS NAV */
.rooms-nav{padding:4rem 2.4rem 1.5rem;display:flex;gap:1.5rem;border-bottom:1px solid var(--surface2);flex-wrap:wrap;align-items:center;justify-content:space-between}
.rooms-tabs{display:flex;gap:2rem;flex-wrap:wrap}
.room-tab{font-size:.6rem;letter-spacing:.3em;text-transform:uppercase;color:var(--neutral);cursor:none;background:none;border:none;padding-bottom:.5rem;border-bottom:1px solid transparent;transition:color .3s,border-color .3s}
.room-tab.active,.room-tab:hover{color:var(--primary);border-color:var(--accent1)}
@media(hover:none){.room-tab{cursor:pointer}}
.gallery-search{font-size:.62rem;letter-spacing:.12em;background:none;border:1px solid var(--surface2);padding:.5rem 1rem;color:var(--primary);font-family:var(--sans);outline:none;width:180px;transition:border-color .3s}
.gallery-search::placeholder{color:var(--neutral)}
.gallery-search:focus{border-color:var(--accent1)}

/* GALLERY */
#gallery{padding:2.5rem 2.4rem 6rem;min-height:70vh}
.gallery-header{margin-bottom:2.5rem}
.gallery-room-title{font-family:var(--serif);font-size:clamp(1.6rem,4vw,2.6rem);font-weight:300;font-style:italic;color:var(--primary);margin-bottom:.3rem}
.gallery-room-sub{font-size:.6rem;letter-spacing:.3em;text-transform:uppercase;color:var(--neutral)}
.grid{display:grid;grid-template-columns:repeat(12,1fr);gap:1.5rem;align-items:start}

.piece{cursor:none;position:relative;transform-style:preserve-3d}
.piece-1{grid-column:span 5}.piece-2{grid-column:span 7}.piece-3{grid-column:span 4}
.piece-4{grid-column:span 4}.piece-5{grid-column:span 4}.piece-6{grid-column:span 6}.piece-7{grid-column:span 6}

.piece-img-wrap{overflow:hidden;background:var(--surface1);position:relative}
.piece-1 .piece-img-wrap{aspect-ratio:3/4}
.piece-2 .piece-img-wrap{aspect-ratio:16/10}
.piece-3 .piece-img-wrap,.piece-4 .piece-img-wrap,.piece-5 .piece-img-wrap{aspect-ratio:3/4}
.piece-6 .piece-img-wrap,.piece-7 .piece-img-wrap{aspect-ratio:4/3}

/* STITCH HOVER EFFECT */
.piece-img-wrap::after{content:"";position:absolute;inset:0;background:repeating-linear-gradient(45deg,transparent,transparent 10px,rgba(179,140,116,.06) 10px,rgba(179,140,116,.06) 11px);opacity:0;transition:opacity .5s;pointer-events:none}
.piece:hover .piece-img-wrap::after{opacity:1}

.piece-img{width:100%;height:100%;object-fit:cover;display:block;transition:transform .9s cubic-bezier(.25,.46,.45,.94);filter:saturate(.88)}
.piece:hover .piece-img{transform:scale(1.06)}
.piece-overlay{position:absolute;inset:0;background:linear-gradient(to top,rgba(73,31,17,.82) 0%,transparent 55%);opacity:0;transition:opacity .5s ease;display:flex;flex-direction:column;justify-content:flex-end;padding:1.5rem}
.piece:hover .piece-overlay{opacity:1}
.overlay-cat{font-size:.52rem;letter-spacing:.3em;text-transform:uppercase;color:var(--surface2);margin-bottom:.3rem}
.overlay-title{font-family:var(--serif);font-size:1.2rem;font-style:italic;color:var(--bg)}
.overlay-act{font-size:.52rem;letter-spacing:.25em;text-transform:uppercase;color:var(--accent1);margin-top:.4rem}
.piece-info{padding:.9rem .1rem .2rem}
.piece-info-top{display:flex;justify-content:space-between;align-items:flex-start;margin-bottom:.15rem}
.piece-name{font-family:var(--serif);font-size:1rem;color:var(--primary)}
.piece-price{font-size:.63rem;letter-spacing:.08em;color:var(--secondary)}
.piece-category-tag{display:inline-block;font-size:.5rem;letter-spacing:.22em;text-transform:uppercase;color:var(--accent2);border:1px solid var(--surface2);padding:.2rem .6rem;margin-bottom:.4rem}

/* HEART */
.wish-btn{background:none;border:none;cursor:none;font-size:1.05rem;line-height:1;color:var(--surface2);transition:color .25s,transform .25s;padding:0 0 0 .5rem;flex-shrink:0;margin-top:-.1rem}
.wish-btn.active{color:#c0464a}
.wish-btn:hover{color:#c0464a}
@media(hover:none){.wish-btn,.piece{cursor:pointer}}

@media(max-width:900px){
  .grid{grid-template-columns:repeat(auto-fit,minmax(260px,1fr))}
  .piece-1,.piece-2,.piece-3,.piece-4,.piece-5,.piece-6,.piece-7{grid-column:span 1}
  .piece-2 .piece-img-wrap{aspect-ratio:4/3}
}
@media(max-width:480px){.grid{grid-template-columns:1fr}.rooms-nav{padding:2rem 1.2rem 1rem;gap:1.2rem}#gallery{padding:1.5rem 1.2rem 4rem}.gallery-search{width:140px}}

/* GLASSMORPHISM MODAL */
#modal{position:fixed;inset:0;z-index:800;opacity:0;visibility:hidden;transition:opacity .5s,visibility .5s;display:grid;grid-template-columns:1fr 1fr;overflow:hidden}
#modal.open{opacity:1;visibility:visible}
.modal-img-side{position:relative;overflow:hidden;background:var(--surface1)}
.modal-img-side img{width:100%;height:100%;object-fit:cover;display:block}
.modal-info-side{padding:5rem 3.5rem;display:flex;flex-direction:column;justify-content:center;overflow-y:auto;background:rgba(236,230,217,.75);backdrop-filter:blur(24px);-webkit-backdrop-filter:blur(24px);border-left:1px solid rgba(179,140,116,.25)}
.modal-close{position:absolute;top:1.2rem;right:1.2rem;background:rgba(236,230,217,.85);backdrop-filter:blur(8px);border:none;cursor:none;font-size:.58rem;letter-spacing:.3em;text-transform:uppercase;color:var(--secondary);z-index:10;padding:.4rem .8rem;transition:color .3s}
.modal-close:hover{color:var(--primary)}
@media(hover:none){.modal-close{cursor:pointer}}
.modal-cat{font-size:.58rem;letter-spacing:.35em;text-transform:uppercase;color:var(--accent2);margin-bottom:1rem}
.modal-title{font-family:var(--serif);font-size:clamp(1.8rem,4vw,2.8rem);font-weight:300;font-style:italic;line-height:1.15;margin-bottom:1rem}
.modal-edition{font-size:.58rem;letter-spacing:.25em;text-transform:uppercase;color:var(--neutral);margin-bottom:1.8rem}
.modal-desc{font-size:.83rem;line-height:1.95;color:var(--secondary);max-width:380px;margin-bottom:2rem}
.modal-price{font-family:var(--serif);font-size:1.5rem;color:var(--primary);margin-bottom:.3rem}
.modal-price-sub{font-size:.58rem;letter-spacing:.2em;text-transform:uppercase;color:var(--neutral);margin-bottom:2rem}
.modal-actions{display:flex;align-items:center;gap:.8rem;flex-wrap:wrap;margin-bottom:.8rem}
.acquire-btn{font-size:.58rem;letter-spacing:.3em;text-transform:uppercase;color:var(--primary);border:1px solid var(--accent1);padding:.9rem 2rem;cursor:none;background:none;transition:background .3s,color .3s}
.acquire-btn:hover{background:var(--primary);color:var(--bg)}
.modal-heart{font-size:1.3rem;color:var(--surface2);background:none;border:1px solid var(--surface2);cursor:none;padding:.6rem .8rem;transition:color .3s,border-color .3s;line-height:1}
.modal-heart.active{color:#c0464a;border-color:#c0464a}
.view-btn{font-size:.58rem;letter-spacing:.25em;text-transform:uppercase;color:var(--secondary);cursor:none;background:none;border:none;text-decoration:underline;text-underline-offset:4px;padding:.9rem 0}
@media(hover:none){.acquire-btn,.view-btn,.modal-heart{cursor:pointer}}
.modal-details{margin-top:2.5rem;padding-top:1.5rem;border-top:1px solid var(--surface2)}
.modal-detail-row{display:flex;justify-content:space-between;padding:.55rem 0;border-bottom:1px solid var(--surface1);font-size:.7rem;color:var(--secondary)}
.modal-detail-row span:first-child{color:var(--neutral);letter-spacing:.1em;text-transform:uppercase;font-size:.58rem}
@media(max-width:900px){#modal{grid-template-columns:1fr;grid-template-rows:45vh 1fr}.modal-info-side{padding:2.5rem 1.5rem 4rem}}

/* JOURNAL */
#journal{padding:6rem 2.4rem;background:var(--surface1)}
.journal-header{display:flex;justify-content:space-between;align-items:flex-end;margin-bottom:3rem;flex-wrap:wrap;gap:1rem}
.journal-title{font-family:var(--serif);font-size:clamp(1.8rem,4vw,2.8rem);font-weight:300;font-style:italic}
.journal-desc{font-size:.75rem;line-height:1.8;color:var(--secondary);max-width:280px;text-align:right}
.journal-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:2rem}
.journal-card{cursor:none;background:var(--bg);overflow:hidden;transition:transform .4s,box-shadow .4s}
.journal-card:hover{transform:translateY(-5px);box-shadow:0 14px 40px rgba(73,31,17,.12)}
@media(hover:none){.journal-card{cursor:pointer}}
.journal-img{width:100%;aspect-ratio:4/3;object-fit:cover;display:block;filter:saturate(.82)}
.journal-body{padding:1.3rem}
.journal-date{font-size:.55rem;letter-spacing:.3em;text-transform:uppercase;color:var(--neutral);margin-bottom:.5rem}
.journal-entry-title{font-family:var(--serif);font-size:1.05rem;font-style:italic;color:var(--primary);margin-bottom:.4rem}
.journal-excerpt{font-size:.73rem;line-height:1.75;color:var(--secondary)}
@media(max-width:900px){.journal-grid{grid-template-columns:1fr 1fr}}
@media(max-width:600px){.journal-grid{grid-template-columns:1fr};#journal{padding:4rem 1.2rem}}

/* CUSTOM ORDER */
#custom{padding:7rem 2.4rem;background:var(--bg);border-top:1px solid var(--surface2)}
.custom-inner{max-width:760px;margin:0 auto}
.custom-label{font-size:.6rem;letter-spacing:.35em;text-transform:uppercase;color:var(--accent2);margin-bottom:1.2rem}
.custom-h2{font-family:var(--serif);font-size:clamp(2rem,3.5vw,3rem);font-weight:300;font-style:italic;line-height:1.2;margin-bottom:.6rem}
.custom-h2 em{color:var(--accent1)}
.custom-body{font-size:.83rem;line-height:2;color:var(--secondary);margin-bottom:2.5rem;max-width:520px}
.custom-form{display:flex;flex-direction:column;gap:1.2rem}
.custom-row{display:grid;grid-template-columns:1fr 1fr;gap:1.2rem}
.custom-field{display:flex;flex-direction:column;gap:.4rem}
.custom-flabel{font-size:.55rem;letter-spacing:.28em;text-transform:uppercase;color:var(--neutral)}
.custom-input,.custom-textarea,.custom-select{background:transparent;border:1px solid var(--surface2);padding:.75rem 1rem;font-size:.82rem;color:var(--primary);font-family:var(--sans);outline:none;transition:border-color .3s;resize:none}
.custom-input:focus,.custom-textarea:focus,.custom-select:focus{border-color:var(--accent1)}
.custom-input::placeholder,.custom-textarea::placeholder{color:var(--neutral)}
.custom-upload-wrap{position:relative;border:1px dashed var(--accent1);padding:1.8rem;text-align:center;transition:background .3s}
.custom-upload-wrap:hover{background:rgba(179,140,116,.07)}
.custom-upload-wrap input[type=file]{position:absolute;inset:0;opacity:0;cursor:pointer;width:100%;height:100%}
.custom-upload-text{font-size:.65rem;letter-spacing:.2em;text-transform:uppercase;color:var(--neutral)}
.custom-upload-preview{display:none;max-width:100%;max-height:180px;object-fit:contain;margin-top:1rem}
.custom-upload-preview.shown{display:block;margin:1rem auto 0}
.custom-submit{align-self:flex-start;font-size:.6rem;letter-spacing:.3em;text-transform:uppercase;color:var(--primary);border:1px solid var(--accent1);padding:.9rem 2.4rem;cursor:none;background:none;transition:background .3s,color .3s}
.custom-submit:hover{background:var(--primary);color:var(--bg)}
@media(hover:none){.custom-submit{cursor:pointer}}
.custom-success{display:none;font-family:var(--serif);font-size:1.1rem;font-style:italic;color:var(--accent1);padding:2rem 0}
@media(max-width:700px){.custom-row{grid-template-columns:1fr}}
@media(max-width:480px){#custom{padding:4rem 1.2rem}}

/* ABOUT */
#about{padding:8rem 2.4rem;display:grid;grid-template-columns:1fr 1fr;gap:6rem;align-items:center}
.about-label{font-size:.6rem;letter-spacing:.35em;text-transform:uppercase;color:var(--accent2);margin-bottom:1.2rem}
.about-h2{font-family:var(--serif);font-size:clamp(2rem,3.5vw,3rem);font-weight:300;line-height:1.2;margin-bottom:1.6rem}
.about-h2 em{font-style:italic;color:var(--accent2)}
.about-body{font-size:.83rem;line-height:2;color:var(--secondary);margin-bottom:1rem}
.about-sig{font-family:var(--serif);font-size:1.2rem;font-style:italic;color:var(--accent1);margin-top:1rem}
.about-right{position:relative}
.about-img-main{width:100%;aspect-ratio:3/4;object-fit:cover;display:block;filter:saturate(.85)}
.about-img-float{position:absolute;width:45%;bottom:-2rem;left:-2rem;aspect-ratio:1;object-fit:cover;display:block;filter:saturate(.82);border:4px solid var(--bg)}
@media(max-width:900px){#about{grid-template-columns:1fr;gap:3rem;padding:5rem 1.5rem}.about-img-float{width:38%;bottom:-1rem;left:-1rem}}
@media(max-width:480px){#about{padding:4rem 1.2rem}}

/* MARQUEE */
.marquee-divider{padding:1.2rem 0;border-top:1px solid var(--surface2);border-bottom:1px solid var(--surface2);overflow:hidden}
.marquee-inner{display:flex;gap:3rem;animation:scrollX 22s linear infinite;white-space:nowrap;align-items:center}
.marquee-text{font-family:var(--serif);font-size:.85rem;font-style:italic;color:var(--neutral);flex-shrink:0}

/* FOOTER */
footer{padding:4rem 2.4rem;background:var(--primary);color:var(--surface1)}
.footer-inner{display:grid;grid-template-columns:1fr 1fr 1fr;gap:3rem;margin-bottom:3rem}
.footer-logo img{height:32px;filter:brightness(0) invert(.6) sepia(.3) saturate(.6);margin-bottom:1.2rem}
.footer-tagline{font-family:var(--serif);font-size:.9rem;font-style:italic;color:var(--accent1);line-height:1.7}
.footer-col-title{font-size:.55rem;letter-spacing:.35em;text-transform:uppercase;color:var(--neutral);margin-bottom:1.1rem}
.footer-links{list-style:none;display:flex;flex-direction:column;gap:.6rem}
.footer-links a{font-size:.73rem;color:var(--surface2);text-decoration:none;transition:color .3s}
.footer-links a:hover{color:var(--surface1)}
.footer-bottom{border-top:1px solid #6b4a3a;padding-top:1.5rem;display:flex;justify-content:space-between;flex-wrap:wrap;gap:.8rem}
.footer-copy{font-size:.58rem;letter-spacing:.13em;color:var(--neutral)}
@media(max-width:900px){.footer-inner{grid-template-columns:1fr 1fr}}
@media(max-width:560px){.footer-inner{grid-template-columns:1fr};footer{padding:3rem 1.2rem}}

/* CART */
#cart{position:fixed;right:0;top:0;bottom:0;width:min(400px,100vw);background:var(--bg);z-index:600;transform:translateX(100%);transition:transform .5s cubic-bezier(.25,.46,.45,.94);padding:5rem 1.8rem 2rem;border-left:1px solid var(--surface2);display:flex;flex-direction:column}
#cart.open{transform:translateX(0)}
.cart-header{display:flex;justify-content:space-between;align-items:center;margin-bottom:1.8rem;padding-bottom:1rem;border-bottom:1px solid var(--surface2)}
.cart-title{font-family:var(--serif);font-size:1.3rem;font-style:italic}
.cart-close{background:none;border:none;cursor:none;font-size:.58rem;letter-spacing:.3em;text-transform:uppercase;color:var(--secondary)}
@media(hover:none){.cart-close{cursor:pointer}}
.cart-items{flex:1;overflow-y:auto}
.cart-item{display:flex;gap:1rem;padding:1rem 0;border-bottom:1px solid var(--surface1)}
.cart-item-img{width:56px;height:68px;object-fit:cover;flex-shrink:0;filter:saturate(.85);display:block}
.cart-item-info{flex:1}
.cart-item-name{font-family:var(--serif);font-size:.9rem;font-style:italic;margin-bottom:.25rem}
.cart-item-price{font-size:.68rem;color:var(--secondary)}
.cart-item-remove{background:none;border:none;cursor:none;font-size:.55rem;color:var(--neutral);margin-top:.35rem;letter-spacing:.1em;text-transform:uppercase}
@media(hover:none){.cart-item-remove{cursor:pointer}}
.cart-footer{padding-top:1.5rem;border-top:1px solid var(--surface2)}
.coupon-wrap{display:flex;gap:.6rem;margin-bottom:.6rem}
.coupon-input{flex:1;background:transparent;border:1px solid var(--surface2);padding:.6rem .9rem;font-size:.68rem;color:var(--primary);font-family:var(--sans);outline:none;transition:border-color .3s}
.coupon-input:focus{border-color:var(--accent1)}
.coupon-input::placeholder{color:var(--neutral)}
.coupon-btn{font-size:.55rem;letter-spacing:.22em;text-transform:uppercase;background:none;border:1px solid var(--accent1);color:var(--primary);padding:.6rem 1rem;cursor:none;transition:background .3s,color .3s;white-space:nowrap}
.coupon-btn:hover{background:var(--primary);color:var(--bg)}
@media(hover:none){.coupon-btn{cursor:pointer}}
.coupon-msg{font-size:.6rem;letter-spacing:.1em;color:var(--accent2);margin-bottom:1rem;min-height:1.2em}
.coupon-msg.err{color:#c0464a}
.cart-total{display:flex;justify-content:space-between;margin-bottom:.5rem}
.cart-total-label{font-size:.58rem;letter-spacing:.25em;text-transform:uppercase;color:var(--neutral)}
.cart-total-val{font-family:var(--serif);font-size:1.05rem}
.discount-row{display:flex;justify-content:space-between;margin-bottom:.5rem}
.discount-label{font-size:.58rem;letter-spacing:.18em;text-transform:uppercase;color:var(--accent2)}
.discount-val{font-size:.78rem;color:var(--accent2)}
.final-row{display:flex;justify-content:space-between;margin-bottom:1.2rem}
.final-label{font-size:.58rem;letter-spacing:.25em;text-transform:uppercase;color:var(--primary);font-weight:500}
.final-val{font-family:var(--serif);font-size:1.1rem;color:var(--primary)}
.checkout-btn{width:100%;padding:.95rem;font-size:.58rem;letter-spacing:.3em;text-transform:uppercase;background:var(--primary);color:var(--bg);border:none;cursor:none;transition:background .3s;margin-top:.5rem}
.checkout-btn:hover{background:var(--accent2)}
@media(hover:none){.checkout-btn{cursor:pointer}}
.cart-empty{text-align:center;padding:3rem 1rem;color:var(--neutral)}
.cart-empty p{font-family:var(--serif);font-style:italic;font-size:.95rem}

/* CHECKOUT MODAL */
#checkoutModal{position:fixed;inset:0;z-index:900;opacity:0;visibility:hidden;transition:opacity .4s;display:flex;align-items:center;justify-content:center;padding:1.5rem}
#checkoutModal.open{opacity:1;visibility:visible}
.checkout-panel{background:rgba(236,230,217,.94);backdrop-filter:blur(28px);-webkit-backdrop-filter:blur(28px);border:1px solid rgba(179,140,116,.3);width:min(520px,100%);max-height:90vh;overflow-y:auto;padding:3.5rem;position:relative;box-shadow:0 40px 80px rgba(73,31,17,.18)}
.checkout-close{position:absolute;top:1.2rem;right:1.2rem;background:none;border:none;cursor:none;font-size:.58rem;letter-spacing:.3em;text-transform:uppercase;color:var(--secondary)}
@media(hover:none){.checkout-close{cursor:pointer}}
.checkout-h{font-family:var(--serif);font-size:1.8rem;font-style:italic;font-weight:300;margin-bottom:.5rem}
.checkout-sub{font-size:.62rem;letter-spacing:.2em;text-transform:uppercase;color:var(--neutral);margin-bottom:2.5rem}
.co-form{display:flex;flex-direction:column;gap:1rem}
.co-field{display:flex;flex-direction:column;gap:.35rem}
.co-label{font-size:.55rem;letter-spacing:.28em;text-transform:uppercase;color:var(--neutral)}
.co-input{background:transparent;border:1px solid var(--surface2);padding:.75rem 1rem;font-size:.82rem;color:var(--primary);font-family:var(--sans);outline:none;transition:border-color .3s}
.co-input:focus{border-color:var(--accent1)}
.co-input.err{border-color:#c0464a}
.co-err{font-size:.58rem;color:#c0464a;letter-spacing:.1em;min-height:1em}
.co-note{font-size:.72rem;line-height:1.75;color:var(--secondary);padding:1rem;background:var(--surface1);margin:.5rem 0}
.co-submit{font-size:.6rem;letter-spacing:.3em;text-transform:uppercase;color:var(--bg);background:var(--primary);border:none;padding:1rem 2.4rem;cursor:none;transition:background .3s;align-self:flex-start;margin-top:.5rem}
.co-submit:hover{background:var(--accent2)}
@media(hover:none){.co-submit{cursor:pointer}}
.co-success{display:none;text-align:center;padding:2rem 0}
.co-success .icon{font-size:2rem;margin-bottom:1rem}
.co-success h3{font-family:var(--serif);font-size:1.5rem;font-style:italic;font-weight:300;margin-bottom:.5rem}
.co-success p{font-size:.78rem;line-height:1.8;color:var(--secondary)}

/* WISHLIST PANEL */
#wishPanel{position:fixed;right:0;top:0;bottom:0;width:min(360px,100vw);background:var(--bg);z-index:601;transform:translateX(100%);transition:transform .5s cubic-bezier(.25,.46,.45,.94);padding:5rem 1.8rem 2rem;border-left:1px solid var(--surface2);display:flex;flex-direction:column}
#wishPanel.open{transform:translateX(0)}
.wp-header{display:flex;justify-content:space-between;align-items:center;margin-bottom:1.8rem;padding-bottom:1rem;border-bottom:1px solid var(--surface2)}
.wp-title{font-family:var(--serif);font-size:1.3rem;font-style:italic}
.wp-close{background:none;border:none;cursor:none;font-size:.58rem;letter-spacing:.3em;text-transform:uppercase;color:var(--secondary)}
@media(hover:none){.wp-close{cursor:pointer}}
.wish-items{flex:1;overflow-y:auto}
.wish-item{display:flex;gap:1rem;padding:1rem 0;border-bottom:1px solid var(--surface1);cursor:none}
@media(hover:none){.wish-item{cursor:pointer}}
.wish-item-img{width:56px;height:68px;object-fit:cover;flex-shrink:0;filter:saturate(.85)}
.wish-item-info{flex:1}
.wish-item-name{font-family:var(--serif);font-size:.9rem;font-style:italic;margin-bottom:.2rem}
.wish-item-price{font-size:.68rem;color:var(--secondary);margin-bottom:.3rem}
.wish-actions{display:flex;gap:.8rem}
.wish-add{background:none;border:none;cursor:none;font-size:.55rem;color:var(--accent2);letter-spacing:.1em;text-transform:uppercase}
.wish-del{background:none;border:none;cursor:none;font-size:.55rem;color:var(--neutral);letter-spacing:.1em;text-transform:uppercase}
@media(hover:none){.wish-add,.wish-del{cursor:pointer}}
.wish-empty{text-align:center;padding:3rem 1rem;color:var(--neutral)}
.wish-empty p{font-family:var(--serif);font-style:italic;font-size:.95rem}

/* OVERLAY */
#overlay-bg{position:fixed;inset:0;background:rgba(73,31,17,.42);z-index:550;opacity:0;visibility:hidden;transition:opacity .4s}
#overlay-bg.show{opacity:1;visibility:visible}

/* REVEAL */
.reveal{opacity:0;transform:translateY(24px);transition:opacity .85s ease,transform .85s ease}
.reveal.visible{opacity:1;transform:none}

/* NOTIF */
.notif{position:fixed;bottom:2rem;left:50%;transform:translateX(-50%) translateY(60px);background:var(--primary);color:var(--bg);font-size:.58rem;letter-spacing:.25em;text-transform:uppercase;padding:.75rem 1.8rem;z-index:700;opacity:0;transition:transform .4s,opacity .4s;white-space:nowrap;max-width:90vw;text-align:center}
.notif.show{transform:translateX(-50%) translateY(0);opacity:1}

/* ANIMATIONS */
@keyframes fadeUp{from{opacity:0;transform:translateY(18px)}to{opacity:1;transform:none}}
@keyframes fadeIn{from{opacity:0}to{opacity:1}}
@keyframes scrollX{from{transform:translateX(0)}to{transform:translateX(-50%)}}
@keyframes heartPop{0%{transform:scale(1)}40%{transform:scale(1.6)}70%{transform:scale(.88)}100%{transform:scale(1)}}
.wish-btn.pop{animation:heartPop .4s ease}

.no-results{grid-column:1/-1;text-align:center;padding:5rem 1rem;font-family:var(--serif);font-size:1.1rem;font-style:italic;color:var(--neutral)}
</style>
</head>
<body>

<div class="cursor" id="cursor" aria-hidden="true"></div>
<div class="cursor-ring" id="cursorRing" aria-hidden="true"></div>
<div class="cursor-thread" id="ct1" style="width:5px;height:5px;background:var(--accent1)" aria-hidden="true"></div>
<div class="cursor-thread" id="ct2" style="width:4px;height:4px;background:var(--accent2)" aria-hidden="true"></div>
<div class="cursor-thread" id="ct3" style="width:3px;height:3px;background:var(--neutral)" aria-hidden="true"></div>
<div class="notif" id="notif" role="status" aria-live="polite"></div>

<!-- ENTRY -->
<div id="entry" role="dialog" aria-label="Exhibition entry screen">
  <div class="entry-label">Exhibition 01 — Now Open</div>
  <img src="https://res.cloudinary.com/dg4vourvl/image/upload/v1777148862/Asna_20260425_204824_0000_iah4pc.png" class="entry-logo" alt="ASNA Needle & Co. logo">
  <div class="entry-title">Where thread becomes testament</div>
  <div class="entry-sub">Handcrafted textile works &nbsp;&middot;&nbsp; Studio edition</div>
  <div class="entry-line" aria-hidden="true"></div>
  <button class="entry-btn" onclick="enterSite()">Enter Exhibition</button>
</div>

<!-- NAV -->
<nav role="navigation" aria-label="Main navigation">
  <a class="nav-logo" href="#" aria-label="ASNA Needle & Co. home">
    <img src="https://res.cloudinary.com/dg4vourvl/image/upload/v1777148862/Asna_20260425_204824_0000_iah4pc.png" alt="ASNA Needle & Co.">
  </a>
  <ul class="nav-links">
    <li><a href="#craft">The Craft</a></li>
    <li><a href="#gallery">Gallery</a></li>
    <li><a href="#custom">Custom Work</a></li>
    <li><a href="#journal">Journal</a></li>
    <li><a href="#about">Studio</a></li>
  </ul>
  <div class="nav-right">
    <button class="nav-btn" onclick="toggleWish()" aria-label="Saved pieces">
      &#9825; Saved
      <span class="badge wish-badge" id="wishCount">0</span>
    </button>
    <button class="nav-btn" onclick="toggleCart()" aria-label="Collection cart">
      Collection
      <span class="badge" id="cartCount">0</span>
    </button>
    <button class="hamburger" id="hamburger" onclick="toggleMenu()" aria-expanded="false">
      <span></span><span></span><span></span>
    </button>
  </div>
</nav>

<div class="mobile-menu" id="mobileMenu">
  <a href="#craft" onclick="closeMenu()">The Craft</a>
  <a href="#gallery" onclick="closeMenu()">Gallery</a>
  <a href="#custom" onclick="closeMenu()">Custom Work</a>
  <a href="#journal" onclick="closeMenu()">Journal</a>
  <a href="#about" onclick="closeMenu()">Studio</a>
</div>

<!-- CRAFT -->
<div id="craft">
  <div class="craft-left">
    <img class="craft-left-img" src="https://res.cloudinary.com/dg4vourvl/image/upload/v1777229444/IMG_20260427_001524_272_q3ym2h.jpg" alt="Hand embroidery in progress at ASNA studio" loading="lazy">
    <div class="craft-left-overlay" aria-hidden="true"></div>
    <div class="craft-left-text">
      <div class="craft-label">The Making</div>
      <div class="craft-title">Every stitch<br>is <em>intentional</em></div>
    </div>
  </div>
  <div class="craft-right reveal">
    <div class="craft-label">ASNA Needle & Co. — Studio Practice</div>
    <p class="craft-body">Before we speak of pieces, we speak of process. Each work begins not with a design brief but with a material conversation — thread against fabric, needle through weave, pressure, patience, repetition.</p>
    <p class="craft-body">We do not manufacture. We make.</p>
    <div class="craft-note">"Craft is the slowest form of love."</div>
    <div class="craft-credit">Threaded works inspired by contemporary embroidery artists reimagining football culture.</div>
  </div>
</div>

<!-- STRIP -->
<div class="process-strip" aria-hidden="true">
  <div class="strip-inner">
    <span class="strip-item">Hand Embroidery</span><span class="strip-dot"></span>
    <span class="strip-item">Crochet Structures</span><span class="strip-dot"></span>
    <span class="strip-item">Surface Calligraphy</span><span class="strip-dot"></span>
    <span class="strip-item">Textile Painting</span><span class="strip-dot"></span>
    <span class="strip-item">Limited Editions</span><span class="strip-dot"></span>
    <span class="strip-item">Custom Commissions</span><span class="strip-dot"></span>
    <span class="strip-item">Hand Embroidery</span><span class="strip-dot"></span>
    <span class="strip-item">Crochet Structures</span><span class="strip-dot"></span>
    <span class="strip-item">Surface Calligraphy</span><span class="strip-dot"></span>
    <span class="strip-item">Textile Painting</span><span class="strip-dot"></span>
    <span class="strip-item">Limited Editions</span><span class="strip-dot"></span>
    <span class="strip-item">Custom Commissions</span><span class="strip-dot"></span>
  </div>
</div>

<!-- ROOMS NAV -->
<div class="rooms-nav" id="roomsNav" role="tablist" aria-label="Gallery rooms">
  <div class="rooms-tabs">
    <button class="room-tab active" role="tab" aria-selected="true" onclick="setRoom('all',this)">All Works</button>
    <button class="room-tab" role="tab" aria-selected="false" onclick="setRoom('threaded',this)">Threaded Works</button>
    <button class="room-tab" role="tab" aria-selected="false" onclick="setRoom('soft',this)">Soft Structures</button>
    <button class="room-tab" role="tab" aria-selected="false" onclick="setRoom('surface',this)">Surface Studies</button>
    <button class="room-tab" role="tab" aria-selected="false" onclick="setRoom('written',this)">Written Forms</button>
    <button class="room-tab" role="tab" aria-selected="false" onclick="setRoom('custom',this)">Custom Works</button>
  </div>
  <input class="gallery-search" id="gallerySearch" type="text" placeholder="Search pieces…" oninput="renderGallery()" aria-label="Search gallery">
</div>

<!-- GALLERY -->
<section id="gallery" aria-label="Exhibition gallery">
  <div class="gallery-header reveal">
    <div class="gallery-room-title" id="roomTitle">All Works</div>
    <div class="gallery-room-sub" id="roomSub">Exhibition 01 — Ongoing Collection</div>
  </div>
  <div class="grid" id="grid" role="list"></div>
</section>

<!-- MARQUEE -->
<div class="marquee-divider" aria-hidden="true">
  <div class="marquee-inner">
    <span class="marquee-text">Studio Works</span><span class="strip-dot"></span>
    <span class="marquee-text">Not for everyone — for the right one</span><span class="strip-dot"></span>
    <span class="marquee-text">Handmade in India</span><span class="strip-dot"></span>
    <span class="marquee-text">Limited Editions</span><span class="strip-dot"></span>
    <span class="marquee-text">ASNA Needle & Co.</span><span class="strip-dot"></span>
    <span class="marquee-text">Studio Works</span><span class="strip-dot"></span>
    <span class="marquee-text">Not for everyone — for the right one</span><span class="strip-dot"></span>
    <span class="marquee-text">Handmade in India</span><span class="strip-dot"></span>
    <span class="marquee-text">Limited Editions</span><span class="strip-dot"></span>
    <span class="marquee-text">ASNA Needle & Co.</span><span class="strip-dot"></span>
  </div>
</div>

<!-- JOURNAL -->
<section id="journal" aria-label="Studio Journal">
  <div class="journal-header reveal">
    <div>
      <div style="font-size:.6rem;letter-spacing:.35em;text-transform:uppercase;color:var(--accent2);margin-bottom:.7rem">Studio Journal</div>
      <div class="journal-title">From the making room</div>
    </div>
    <div class="journal-desc">A creative archive of process, experiments, and unfinished thoughts. Not a shop — a studio.</div>
  </div>
  <div class="journal-grid" id="journalGrid"></div>
</section>

<!-- CUSTOM ORDER -->
<section id="custom" aria-label="Custom commission">
  <div class="custom-inner">
    <div class="custom-label reveal">Custom Works</div>
    <h2 class="custom-h2 reveal">Commission something<br><em>made only for you</em></h2>
    <p class="custom-body reveal">Every piece in this gallery started with an idea. If you carry one — a jersey number, a memory, a colour that won't leave you — send it. We will make it thread-by-thread, from nothing.</p>
    <form class="custom-form reveal" id="customForm" onsubmit="submitCustom(event)">
      <div class="custom-row">
        <div class="custom-field">
          <label class="custom-flabel" for="cName">Your Name *</label>
          <input class="custom-input" id="cName" type="text" placeholder="Asna Khan" required autocomplete="name">
        </div>
        <div class="custom-field">
          <label class="custom-flabel" for="cPhone">Phone Number *</label>
          <input class="custom-input" id="cPhone" type="tel" placeholder="+91 98000 00000" required autocomplete="tel">
        </div>
      </div>
      <div class="custom-field">
        <label class="custom-flabel" for="cEmail">Email <span style="color:var(--neutral);font-style:italic">(optional)</span></label>
        <input class="custom-input" id="cEmail" type="email" placeholder="your@email.com" autocomplete="email">
      </div>
      <div class="custom-field">
        <label class="custom-flabel" for="cCat">Category</label>
        <select class="custom-input custom-select" id="cCat">
          <option value="threaded">Threaded Works — Football Embroidery</option>
          <option value="soft">Soft Structures — Crochet</option>
          <option value="surface">Surface Studies — Painting</option>
          <option value="written">Written Forms — Calligraphy</option>
        </select>
      </div>
      <div class="custom-field">
        <label class="custom-flabel" for="cDesc">Your Vision *</label>
        <textarea class="custom-textarea" id="cDesc" rows="5" placeholder="Tell us about the piece — a player, a colour, a feeling. The more you share, the more alive it becomes." required></textarea>
      </div>
      <div class="custom-field">
        <label class="custom-flabel">Reference Image <span style="color:var(--neutral);font-style:italic">(optional)</span></label>
        <div class="custom-upload-wrap">
          <input type="file" accept="image/*" id="cImg" onchange="previewImg(event)">
          <div class="custom-upload-text" id="uploadTxt">↑ Drop an image or click to browse</div>
          <img class="custom-upload-preview" id="uploadPrev" alt="Reference preview">
        </div>
      </div>
      <button type="submit" class="custom-submit">Send Commission Brief</button>
    </form>
    <div class="custom-success" id="customOk">
      <div style="font-size:2rem;margin-bottom:.5rem">✦</div>
      Your brief has been received. The studio will be in touch within 48 hours.
    </div>
  </div>
</section>

<!-- ABOUT -->
<section id="about" aria-label="About the studio">
  <div class="about-left reveal">
    <div class="about-label">The Studio</div>
    <h2 class="about-h2">A practice built on<br><em>slow making</em></h2>
    <p class="about-body">ASNA Needle & Co. is a one-woman design studio rooted in the belief that handcraft is the most honest form of expression. Every piece made here is stitched by hand, considered over time, and released only when it is ready.</p>
    <p class="about-body">We work at the intersection of football culture and textile art — not as a commercial venture, but as a personal investigation into what sport means when translated through thread, crochet, and paint.</p>
    <div class="about-sig">— Asna, Studio Founder</div>
  </div>
  <div class="about-right reveal">
    <img class="about-img-main" src="https://res.cloudinary.com/dg4vourvl/image/upload/v1777229428/IMG_20260427_001558_799_skgxei.jpg" alt="ASNA studio work in progress" loading="lazy">
    <img class="about-img-float" src="https://res.cloudinary.com/dg4vourvl/image/upload/v1777229420/IMG_20260427_001558_455_tqdfdy.jpg" alt="Close detail of hand stitching" loading="lazy">
  </div>
</section>

<!-- FOOTER -->
<footer>
  <div class="footer-inner">
    <div>
      <div class="footer-logo"><img src="https://res.cloudinary.com/dg4vourvl/image/upload/v1777148862/Asna_20260425_204824_0000_iah4pc.png" alt="ASNA"></div>
      <div class="footer-tagline">Handmade textile works.<br>Studio practice. Limited editions.</div>
    </div>
    <div>
      <div class="footer-col-title">Navigate</div>
      <ul class="footer-links">
        <li><a href="#craft">The Craft</a></li>
        <li><a href="#gallery">Exhibition Gallery</a></li>
        <li><a href="#custom">Custom Commission</a></li>
        <li><a href="#journal">Studio Journal</a></li>
        <li><a href="#about">About</a></li>
      </ul>
    </div>
    <div>
      <div class="footer-col-title">Contact</div>
      <ul class="footer-links">
        <li><a href="mailto:studio@asnaneedle.com">studio@asnaneedle.com</a></li>
        <li><a href="#custom">Commission Inquiries</a></li>
        <li><a href="#">Care & Handling</a></li>
      </ul>
    </div>
  </div>
  <div class="footer-bottom">
    <div class="footer-copy">© 2026 ASNA Needle & Co. — All works handmade in India</div>
    <div class="footer-copy">Not mass produced. Not replicated. Not rushed.</div>
  </div>
</footer>

<!-- MODAL -->
<div id="modal" role="dialog" aria-modal="true" aria-label="Piece detail">
  <div class="modal-img-side" id="modalImg">
    <button class="modal-close" onclick="closeModal()">✕ Close</button>
  </div>
  <div class="modal-info-side" id="modalInfo"></div>
</div>

<!-- CHECKOUT -->
<div id="checkoutModal" role="dialog" aria-modal="true" aria-label="Checkout">
  <div class="checkout-panel">
    <button class="checkout-close" onclick="closeCheckout()">✕ Close</button>
    <div id="coFormWrap">
      <div class="checkout-h">Commission Details</div>
      <div class="checkout-sub">A few details before we proceed</div>
      <div class="co-form">
        <div class="co-field">
          <label class="co-label" for="coName">Full Name *</label>
          <input class="co-input" id="coName" type="text" placeholder="Your name" autocomplete="name">
          <span class="co-err" id="coNameE"></span>
        </div>
        <div class="co-field">
          <label class="co-label" for="coPhone">Phone Number *</label>
          <input class="co-input" id="coPhone" type="tel" placeholder="+91 98000 00000" autocomplete="tel">
          <span class="co-err" id="coPhoneE"></span>
        </div>
        <div class="co-field">
          <label class="co-label" for="coEmail">Email <span style="font-style:italic;color:var(--neutral)">(optional)</span></label>
          <input class="co-input" id="coEmail" type="email" placeholder="your@email.com" autocomplete="email">
        </div>
        <div class="co-field">
          <label class="co-label" for="coAddr">Delivery Address *</label>
          <input class="co-input" id="coAddr" type="text" placeholder="City, State, PIN" autocomplete="address-line1">
          <span class="co-err" id="coAddrE"></span>
        </div>
        <div class="co-field">
          <label class="co-label" for="coNotes">Special Notes <span style="font-style:italic;color:var(--neutral)">(optional)</span></label>
          <input class="co-input" id="coNotes" type="text" placeholder="Gifting, framing, etc.">
        </div>
        <div class="co-note">All acquisitions are handled personally. You will receive confirmation within 48 hours with payment details via WhatsApp or email.</div>
        <button class="co-submit" onclick="submitCheckout()">Confirm Commission</button>
      </div>
    </div>
    <div class="co-success" id="coSuccess">
      <div class="icon">✦</div>
      <h3>Commission Confirmed</h3>
      <p>Thank you. The studio will be in touch within 48 hours to confirm your acquisition and share payment details.</p>
    </div>
  </div>
</div>

<!-- OVERLAY -->
<div id="overlay-bg" onclick="closeAll()" aria-hidden="true"></div>

<!-- CART -->
<div id="cart" role="complementary" aria-label="Collection cart">
  <div class="cart-header">
    <div class="cart-title">Your Collection</div>
    <button class="cart-close" onclick="toggleCart()">Close ✕</button>
  </div>
  <div class="cart-items" id="cartItems"></div>
  <div class="cart-footer" id="cartFooter"></div>
</div>

<!-- WISHLIST -->
<div id="wishPanel" role="complementary" aria-label="Saved pieces">
  <div class="wp-header">
    <div class="wp-title">Saved Pieces</div>
    <button class="wp-close" onclick="toggleWish()">Close ✕</button>
  </div>
  <div class="wish-items" id="wishItems"></div>
</div>

<script>
/* PIECES DATA */
const pieces = [
  {id:1,name:"Azzurri Form",cat:"threaded",catLabel:"Threaded Works",edition:"Edition of 3",price:"₹3,800",priceInt:3800,priceSub:"Acquisition — Studio Piece",desc:"An embroidered study of a classic football silhouette in motion. DMC silk on organic linen, worked freehand without a hoop.",dims:"28 × 38 cm",material:"Linen, DMC silk thread",time:"11 hours",class:"piece-1",img:"https://res.cloudinary.com/dg4vourvl/image/upload/v1777229444/IMG_20260427_001524_272_q3ym2h.jpg",fallback:"linear-gradient(150deg,#c4a882,#a07d60,#d4b898)"},
  {id:2,name:"Dual Stripe Study",cat:"threaded",catLabel:"Threaded Works",edition:"Unique Work",price:"₹5,800",priceInt:5800,priceSub:"Acquisition — Original Piece",desc:"A panoramic embroidered work exploring the geometry of a football strip.",dims:"52 × 32 cm",material:"Cotton canvas, wool & silk thread",time:"19 hours",class:"piece-2",img:"https://res.cloudinary.com/dg4vourvl/image/upload/v1777229438/IMG_20260427_001524_648_gunbxq.jpg",fallback:"linear-gradient(120deg,#8b7363,#b38c74,#ece6d9)"},
  {id:3,name:"Night Crest",cat:"threaded",catLabel:"Threaded Works",edition:"Edition of 5",price:"₹2,699",priceInt:2699,priceSub:"Studio Piece",desc:"A keeper's emblem worked in deep indigo and rust.",dims:"22 × 30 cm",material:"Hand-dyed linen, cotton thread",time:"8 hours",class:"piece-3",img:"https://res.cloudinary.com/dg4vourvl/image/upload/v1777229444/IMG_20260427_001524_622_vxjcmx.jpg",fallback:"linear-gradient(135deg,#caa892,#946a53,#e4cdb9)"},
  {id:4,name:"Terrace Fragment",cat:"threaded",catLabel:"Threaded Works",edition:"Edition of 4",price:"₹3,200",priceInt:3200,priceSub:"Studio Piece",desc:"The crowd distilled to texture.",dims:"30 × 30 cm",material:"Linen, French cotton thread",time:"12 hours",class:"piece-4",img:"https://res.cloudinary.com/dg4vourvl/image/upload/v1777229437/IMG_20260427_001524_145_wtlaxj.jpg",fallback:"linear-gradient(135deg,#e4cdb9,#b38c74,#ece6d9)"},
  {id:5,name:"Half-Time Light",cat:"threaded",catLabel:"Threaded Works",edition:"Unique Work",price:"₹4,400",priceInt:4400,priceSub:"Acquisition",desc:"The quality of light at half-time.",dims:"25 × 35 cm",material:"Cotton canvas, mulberry silk",time:"14 hours",class:"piece-5",img:"https://res.cloudinary.com/dg4vourvl/image/upload/v1777229436/IMG_20260427_001530_122_nw9qfx.jpg",fallback:"linear-gradient(160deg,#946a53,#caa892,#8b7363)"},
  {id:6,name:"The Forward's Angle",cat:"threaded",catLabel:"Threaded Works",edition:"Edition of 3",price:"₹3,500",priceInt:3500,priceSub:"Studio Piece",desc:"Geometry of attack.",dims:"40 × 30 cm",material:"Linen, silk thread, cotton backing",time:"10 hours",class:"piece-6",img:"https://res.cloudinary.com/dg4vourvl/image/upload/v1777229435/IMG_20260427_001529_930_sqdxzl.jpg",fallback:"linear-gradient(135deg,#b38c74,#491f11,#caa892)"},
  {id:7,name:"Mudlark Study",cat:"threaded",catLabel:"Threaded Works",edition:"Edition of 5",price:"₹2,999",priceInt:2999,priceSub:"Studio Piece",desc:"An embroidered portrait of the pitch itself.",dims:"28 × 28 cm",material:"Raw linen, cotton DMC thread",time:"9 hours",class:"piece-7",img:"https://res.cloudinary.com/dg4vourvl/image/upload/v1777229430/IMG_20260427_001530_145_d8endg.jpg",fallback:"linear-gradient(135deg,#8b7363,#491f11,#726054)"},
  {id:8,name:"Stripe No. 9",cat:"threaded",catLabel:"Threaded Works",edition:"Unique Work",price:"₹5,200",priceInt:5200,priceSub:"Acquisition — Original",desc:"The number nine is a mythology.",dims:"32 × 40 cm",material:"Raw cotton, silk and wool thread",time:"16 hours",class:"piece-1",img:"https://res.cloudinary.com/dg4vourvl/image/upload/v1777229430/IMG_20260427_001530_021_gsogau.jpg",fallback:"linear-gradient(150deg,#c4a882,#a07d60,#d4b898)"},

  {id:9,name:"Lattice Form I",cat:"soft",catLabel:"Soft Structures",edition:"Edition of 4",price:"₹1,899",priceInt:1899,priceSub:"Studio Piece",desc:"Crochet panel in undyed merino wool.",dims:"30 × 30 cm",material:"Undyed merino wool",time:"6 hours",class:"piece-2",img:"https://res.cloudinary.com/dg4vourvl/image/upload/v1777229428/IMG_20260427_001558_799_skgxei.jpg",fallback:"linear-gradient(135deg,#e4cdb9,#b38c74,#ece6d9)"},
  {id:10,name:"Open Weave No. 2",cat:"soft",catLabel:"Soft Structures",edition:"Unique Work",price:"₹2,200",priceInt:2200,priceSub:"Acquisition",desc:"Experimental crochet exploring negative space.",dims:"25 × 35 cm",material:"Cotton cord, linen yarn",time:"7 hours",class:"piece-3",img:"https://res.cloudinary.com/dg4vourvl/image/upload/v1777229420/IMG_20260427_001558_455_tqdfdy.jpg",fallback:"linear-gradient(160deg,#946a53,#caa892,#8b7363)"},
  {id:11,name:"Soft Ground",cat:"soft",catLabel:"Soft Structures",edition:"Edition of 3",price:"₹2,500",priceInt:2500,priceSub:"Studio Piece",desc:"A crochet textile panel built around worn pitch markings.",dims:"35 × 25 cm",material:"Cotton, natural yarn",time:"8 hours",class:"piece-4",img:"https://res.cloudinary.com/dg4vourvl/image/upload/v1777229428/IMG_20260427_001558_820_bsk1em.jpg",fallback:"linear-gradient(135deg,#caa892,#946a53,#e4cdb9)"},

  {id:12,name:"Field Wash — Ochre",cat:"surface",catLabel:"Surface Studies",edition:"Unique Work",price:"₹4,100",priceInt:4100,priceSub:"Acquisition — Original",desc:"Textile painting on raw cotton.",dims:"40 × 50 cm",material:"Raw cotton, natural earth pigments",time:"4 sessions",class:"piece-5",img:"https://res.cloudinary.com/dg4vourvl/image/upload/v1777229420/IMG_20260427_001558_870_yjuoyk.jpg",fallback:"linear-gradient(135deg,#b38c74,#491f11,#caa892)"},
  {id:13,name:"Rust Ground Study",cat:"surface",catLabel:"Surface Studies",edition:"Edition of 2",price:"₹3,800",priceInt:3800,priceSub:"Studio Piece",desc:"Iron-oxide pigment on undyed cotton.",dims:"35 × 45 cm",material:"Cotton, iron-oxide pigment",time:"3 sessions",class:"piece-6",img:"https://res.cloudinary.com/dg4vourvl/image/upload/v1777229420/IMG_20260427_001558_913_d82bko.jpg",fallback:"linear-gradient(135deg,#caa892,#946a53,#ece6d9)"},
  {id:14,name:"Pale Ground, Margin",cat:"surface",catLabel:"Surface Studies",edition:"Unique Work",price:"₹4,500",priceInt:4500,priceSub:"Acquisition — Original",desc:"A washed surface study in near-white tones.",dims:"50 × 40 cm",material:"Handwoven cotton, natural dye",time:"5 sessions",class:"piece-7",img:"https://res.cloudinary.com/dg4vourvl/image/upload/v1777229420/IMG_20260427_001558_651_wolzgz.jpg",fallback:"linear-gradient(160deg,#e4cdb9,#b38c74,#ece6d9)"},

  {id:15,name:"Alif — In Rust",cat:"written",catLabel:"Written Forms",edition:"Edition of 6",price:"₹1,650",priceInt:1650,priceSub:"Studio Piece",desc:"Calligraphic study of the letter Alif.",dims:"18 × 26 cm",material:"Linen, iron gall ink, reed pen",time:"3 hours",class:"piece-1",img:"https://res.cloudinary.com/dg4vourvl/image/upload/v1777229418/IMG_20260427_001624_818_g8fy4o.jpg",fallback:"linear-gradient(135deg,#8b7363,#491f11,#726054)"},
  {id:16,name:"Naam — Dark Ground",cat:"written",catLabel:"Written Forms",edition:"Unique Work",price:"₹2,800",priceInt:2800,priceSub:"Acquisition",desc:"The Urdu word for 'name' written on tea-stained cotton.",dims:"24 × 32 cm",material:"Tea-stained cotton, india ink",time:"4 hours",class:"piece-2",img:"https://res.cloudinary.com/dg4vourvl/image/upload/v1777229418/IMG_20260427_001624_374_yxmg2r.jpg",fallback:"linear-gradient(120deg,#491f11,#726054,#946a53)"},
  {id:17,name:"Khat Fragment",cat:"written",catLabel:"Written Forms",edition:"Edition of 5",price:"₹1,400",priceInt:1400,priceSub:"Studio Piece",desc:"A fragment of Khat calligraphy.",dims:"16 × 22 cm",material:"Muslin, bamboo pen, ink",time:"2 hours",class:"piece-3",img:"https://res.cloudinary.com/dg4vourvl/image/upload/v1777229418/IMG_20260427_001624_776_hnhmhu.jpg",fallback:"linear-gradient(135deg,#8b7363,#caa892,#726054)"},

  {id:18,name:"Commission — Amber Form",cat:"custom",catLabel:"Custom Works",edition:"One-of-One",price:"₹6,500+",priceInt:6500,priceSub:"Commission — Enquire",desc:"A completed custom embroidery commission.",dims:"Variable",material:"To specification",time:"Variable",class:"piece-4",img:"https://res.cloudinary.com/dg4vourvl/image/upload/v1777229405/IMG_20260427_001751_669_z7s5hi.jpg",fallback:"linear-gradient(135deg,#c4a882,#a07d60,#d4b898)"},
  {id:19,name:"Commission — Soft Portrait",cat:"custom",catLabel:"Custom Works",edition:"One-of-One",price:"₹7,200+",priceInt:7200,priceSub:"Commission — Enquire",desc:"A portrait commission worked in crochet and textile paint.",dims:"Variable",material:"To specification",time:"Variable",class:"piece-5",img:"https://res.cloudinary.com/dg4vourvl/image/upload/v1777229394/IMG_20260427_001751_610_yhhw9q.jpg",fallback:"linear-gradient(150deg,#946a53,#caa892,#8b7363)"},
  {id:20,name:"Commission — Thread Map",cat:"custom",catLabel:"Custom Works",edition:"One-of-One",price:"₹5,800+",priceInt:5800,priceSub:"Commission — Enquire",desc:"A custom topographic embroidery.",dims:"Variable",material:"To specification",time:"Variable",class:"piece-6",img:"https://res.cloudinary.com/dg4vourvl/image/upload/v1777229393/IMG_20260427_001751_854_ga3utd.jpg",fallback:"linear-gradient(135deg,#b38c74,#491f11,#caa892)"},
  {id:21,name:"Commission — Calligraphic Name",cat:"custom",catLabel:"Custom Works",edition:"One-of-One",price:"₹3,200+",priceInt:3200,priceSub:"Commission — Enquire",desc:"A personalised calligraphy commission.",dims:"Variable",material:"To specification",time:"Variable",class:"piece-7",img:"https://res.cloudinary.com/dg4vourvl/image/upload/v1777229392/IMG_20260427_001751_686_k1p7xv.jpg",fallback:"linear-gradient(135deg,#8b7363,#491f11,#726054)"},
  {id:22,name:"Commission — Kit Study",cat:"custom",catLabel:"Custom Works",edition:"One-of-One",price:"₹4,500+",priceInt:4500,priceSub:"Commission — Enquire",desc:"A custom football kit embroidery.",dims:"Variable",material:"To specification",time:"Variable",class:"piece-1",img:"https://res.cloudinary.com/dg4vourvl/image/upload/v1777229391/IMG_20260427_001751_272_diiigb.jpg",fallback:"linear-gradient(150deg,#c4a882,#a07d60,#d4b898)"}
];

const journals = [
  {date:"March 2026",title:"On beginning without a plan",excerpt:"The best embroidery I have ever made started with a knot and no idea.",img:"https://res.cloudinary.com/dg4vourvl/image/upload/v1777229444/IMG_20260427_001524_272_q3ym2h.jpg"},
  {date:"February 2026",title:"Thread tension & patience",excerpt:"I spent three weeks working on one square centimetre.",img:"https://res.cloudinary.com/dg4vourvl/image/upload/v1777229428/IMG_20260427_001558_820_bsk1em.jpg"},
  {date:"January 2026",title:"Calligraphy as warm-up",excerpt:"Every studio day begins with writing.",img:"https://res.cloudinary.com/dg4vourvl/image/upload/v1777229418/IMG_20260427_001624_818_g8fy4o.jpg"}
];

const COUPONS = {
  ASNA10: {type:"pct",val:10,label:"10% off applied"},
  STITCH20: {type:"pct",val:20,label:"20% off applied"},
  GIFT500: {type:"flat",val:500,label:"₹500 gift credit applied"},
  FIRSTWORK: {type:"pct",val:15,label:"15% welcome discount applied"}
};

let cart = [], currentRoom = "all", discount = null;

/* Wishlist */
function getWL(){try{return JSON.parse(localStorage.getItem("asna_wl")||"[]")}catch{return[]}}
function saveWL(a){try{localStorage.setItem("asna_wl",JSON.stringify(a))}catch{}}
function isWL(id){return getWL().includes(id)}
function toggleWishlist(id,fromModal){
  const a = getWL(), i = a.indexOf(id), added = i===-1;
  if(added) a.push(id); else a.splice(i,1);
  saveWL(a);
  updateWishBadge();
  document.querySelectorAll(`.wish-btn[data-id="${id}"]`).forEach(b=>{
    b.textContent = added ? "♥" : "♡";
    b.classList.toggle("active", added);
    if(added){b.classList.add("pop"); setTimeout(()=>b.classList.remove("pop"),450)}
  });
  if(fromModal){
    const mb = document.getElementById("mHeart");
    if(mb){ mb.textContent = added ? "♥" : "♡"; mb.classList.toggle("active", added); }
  }
  if(document.getElementById("wishPanel").classList.contains("open")) renderWishItems();
  notify(added ? "♥ Saved to wishlist" : "Removed from wishlist");
}
function updateWishBadge(){
  const c = getWL().length, el = document.getElementById("wishCount");
  el.textContent = c; el.classList.toggle("has", c>0);
}

/* Cursor */
const cur = document.getElementById("cursor"), ring = document.getElementById("cursorRing");
const threads = [document.getElementById("ct1"), document.getElementById("ct2"), document.getElementById("ct3")];
let trail = [];
const isTouch = () => window.matchMedia("(hover:none)").matches;
document.addEventListener("mousemove", e => {
  if(isTouch()) return;
  const x = e.clientX, y = e.clientY;
  cur.style.left = x+"px"; cur.style.top = y+"px";
  ring.style.left = x+"px"; ring.style.top = y+"px";
  trail.unshift({x,y}); if(trail.length>3) trail.pop();
  threads.forEach((t,i) => {
    const p = trail[i]; if(!p) return;
    t.style.left = p.x+"px"; t.style.top = p.y+"px";
    t.style.opacity = (0.35 - i*0.1).toString();
  });
});
document.addEventListener("mouseover", e => {
  if(isTouch()) return;
  const h = e.target.closest("button,a,.piece,.journal-card,.room-tab");
  cur.classList.toggle("hover", !!h); ring.classList.toggle("hover", !!h);
});

/* Entry */
function enterSite(){ document.getElementById("entry").classList.add("hidden"); }

/* Menu */
function toggleMenu(){
  const b = document.getElementById("hamburger"), m = document.getElementById("mobileMenu");
  const o = m.classList.toggle("open");
  b.classList.toggle("open", o);
  b.setAttribute("aria-expanded", o);
}
function closeMenu(){ document.getElementById("hamburger").classList.remove("open"); document.getElementById("mobileMenu").classList.remove("open"); }

/* Rooms */
const rTitles = {all:"All Works",threaded:"Threaded Works",soft:"Soft Structures",surface:"Surface Studies",written:"Written Forms",custom:"Custom Works"};
const rSubs = {all:"Exhibition 01 — Ongoing Collection",threaded:"Hand embroidery — football-inspired works",soft:"Crochet — structural textile explorations",surface:"Painting-based textile & visual works",written:"Calligraphy works on textile & paper",custom:"Commission pieces — made only for you"};

function setRoom(r,btn){
  currentRoom = r;
  document.querySelectorAll(".room-tab").forEach(t=>{ t.classList.remove("active"); t.setAttribute("aria-selected","false"); });
  btn.classList.add("active"); btn.setAttribute("aria-selected","true");
  document.getElementById("roomTitle").textContent = rTitles[r];
  document.getElementById("roomSub").textContent = rSubs[r];
  document.getElementById("gallerySearch").value = "";
  renderGallery();
}

/* Gallery */
function renderGallery(){
  const grid = document.getElementById("grid");
  const q = (document.getElementById("gallerySearch")?.value || "").toLowerCase().trim();
  let list = currentRoom==="all" ? pieces : pieces.filter(p => p.cat === currentRoom);
  if(q) list = list.filter(p => p.name.toLowerCase().includes(q) || p.catLabel.toLowerCase().includes(q) || p.desc.toLowerCase().includes(q));
  
  if(!list.length){
    grid.innerHTML = `<div class='no-results'>No pieces found — try a different search or category.</div>`;
    return;
  }

  const wl = getWL();
  grid.innerHTML = list.map(p => `
    <div class="piece \( {p.class} reveal" onclick="openModal( \){p.id})" role="listitem" tabindex="0" onkeydown="if(event.key==='Enter')openModal(${p.id})" aria-label="View ${p.name}">
      <div class="piece-img-wrap" style="background:${p.fallback}">
        <img src="\( {p.img}" class="piece-img" alt=" \){p.name}" loading="lazy">
        <div class="piece-overlay" aria-hidden="true">
          <div class="overlay-cat">${p.catLabel}</div>
          <div class="overlay-title">${p.name}</div>
          <div class="overlay-act">View Piece →</div>
        </div>
      </div>
      <div class="piece-info">
        <div class="piece-category-tag">${p.catLabel}</div>
        <div class="piece-info-top">
          <div class="piece-name">${p.name}</div>
          <button class="wish-btn \( {wl.includes(p.id)?'active':''}" data-id=" \){p.id}" onclick="event.stopPropagation();toggleWishlist(\( {p.id},false)" aria-label="Save"> \){wl.includes(p.id)?'♥':'♡'}</button>
        </div>
        <div class="piece-price">${p.price} — ${p.edition}</div>
      </div>
    </div>`).join("");

  document.querySelectorAll(".reveal").forEach(el => observer.observe(el));
}

/* Modal */
function openModal(id){
  const p = pieces.find(x=>x.id===id); if(!p) return;
  const inWL = isWL(id);
  document.getElementById("modalImg").innerHTML = `
    <button class="modal-close" onclick="closeModal()">✕ Close</button>
    <img src="\( {p.img}" alt=" \){p.name}" style="width:100%;height:100%;object-fit:cover">`;
  document.getElementById("modalInfo").innerHTML = `
    <div class="modal-cat">${p.catLabel}</div>
    <div class="modal-title">${p.name}</div>
    <div class="modal-edition">${p.edition}</div>
    <div class="modal-desc">${p.desc}</div>
    <div class="modal-price">${p.price}</div>
    <div class="modal-price-sub">${p.priceSub}</div>
    <div class="modal-actions">
      <button class="acquire-btn" onclick="addToCart(${p.id})">Acquire This Piece</button>
      <button class="modal-heart \( {inWL?'active':''}" id="mHeart" onclick="toggleWishlist( \){p.id},true)">${inWL?'♥':'♡'}</button>
    </div>
    <button class="view-btn" onclick="closeModal()">Return to Gallery</button>
    <div class="modal-details">
      <div class="modal-detail-row"><span>Dimensions</span><span>${p.dims}</span></div>
      <div class="modal-detail-row"><span>Material</span><span>${p.material}</span></div>
      <div class="modal-detail-row"><span>Time Invested</span><span>${p.time}</span></div>
      <div class="modal-detail-row"><span>Edition</span><span>${p.edition}</span></div>
    </div>`;
  document.getElementById("modal").classList.add("open");
  document.getElementById("overlay-bg").classList.add("show");
  document.body.style.overflow = "hidden";
}
function closeModal(){
  document.getElementById("modal").classList.remove("open");
  document.getElementById("overlay-bg").classList.remove("show");
  document.body.style.overflow = "";
}

/* Cart */
function addToCart(id){
  const p = pieces.find(x=>x.id===id);
  if(!p || cart.find(c=>c.id===id)){ notify("Already in your collection"); return; }
  cart.push(p);
  renderCart();
  notify("Added to your collection ✦");
  closeModal();
}
function removeFromCart(id){ cart = cart.filter(c=>c.id!==id); renderCart(); }
function getTotal(){ return cart.reduce((s,p)=>s+(p.priceInt||0),0); }
function getFinal(){ 
  const t = getTotal();
  if(!discount) return t;
  return discount.type==="pct" ? Math.round(t*(1-discount.val/100)) : Math.max(0, t-discount.val);
}
function fmt(n){ return "₹"+n.toLocaleString("en-IN"); }

function renderCart(){
  document.getElementById("cartCount").textContent = cart.length;
  const el = document.getElementById("cartItems"), ft = document.getElementById("cartFooter");
  if(!cart.length){
    el.innerHTML = `<div class='cart-empty'><p>Your collection is empty</p></div>`;
    ft.innerHTML = ""; discount = null; return;
  }
  el.innerHTML = cart.map(p => `
    <div class="cart-item">
      <img class="cart-item-img" src="\( {p.img}" alt=" \){p.name}">
      <div class="cart-item-info">
        <div class="cart-item-name">${p.name}</div>
        <div class="cart-item-price">${p.price}</div>
        <button class="cart-item-remove" onclick="removeFromCart(${p.id})">Remove</button>
      </div>
    </div>`).join("");

  const raw = getTotal(), final = getFinal();
  ft.innerHTML = `
    <div class="coupon-wrap">
      <input class="coupon-input" id="couponIn" type="text" placeholder="Gift or promo code">
      <button class="coupon-btn" onclick="applyCoupon()">Apply</button>
    </div>
    <div class="coupon-msg" id="couponMsg">${discount ? "✓ " + discount.label : ""}</div>
    <div class="cart-total">
      <span class="cart-total-label">Total</span>
      <span class="cart-total-val">${fmt(raw)}</span>
    </div>
    \( {discount ? `<div class="final-row"><span class="final-label">After Discount</span><span class="final-val"> \){fmt(final)}</span></div>` : ''}
    <button class="checkout-btn" onclick="openCheckout()">Proceed to Commission</button>`;
}
function applyCoupon(){
  const code = (document.getElementById("couponIn").value || "").trim().toUpperCase();
  const msg = document.getElementById("couponMsg");
  if(COUPONS[code]){
    discount = {...COUPONS[code], code};
    msg.innerHTML = "✓ " + discount.label;
    msg.className = "coupon-msg";
    renderCart();
  } else {
    msg.innerHTML = "Invalid code.";
    msg.className = "coupon-msg err";
  }
}

function toggleCart(){
  const c = document.getElementById("cart"), ob = document.getElementById("overlay-bg");
  const o = c.classList.toggle("open");
  ob.classList.toggle("show", o);
  document.body.style.overflow = o ? "hidden" : "";
}

function toggleWish(){
  const wp = document.getElementById("wishPanel"), ob = document.getElementById("overlay-bg");
  const o = wp.classList.toggle("open");
  ob.classList.toggle("show", o);
  document.body.style.overflow = o ? "hidden" : "";
  if(o) renderWishItems();
}

function renderWishItems(){
  const list = getWL().map(id => pieces.find(p=>p.id===id)).filter(Boolean);
  const el = document.getElementById("wishItems");
  if(!list.length){
    el.innerHTML = `<div class='wish-empty'><p>Nothing saved yet</p></div>`;
    return;
  }
  el.innerHTML = list.map(p => `
    <div class="wish-item" onclick="openFromWish(${p.id})">
      <img class="wish-item-img" src="\( {p.img}" alt=" \){p.name}">
      <div class="wish-item-info">
        <div class="wish-item-name">${p.name}</div>
        <div class="wish-item-price">${p.price}</div>
        <div class="wish-actions">
          <button class="wish-add" onclick="event.stopPropagation();addToCart(${p.id})">Add to Collection</button>
          <button class="wish-del" onclick="event.stopPropagation();toggleWishlist(${p.id},false)">Remove</button>
        </div>
      </div>
    </div>`).join("");
}
function openFromWish(id){
  document.getElementById("wishPanel").classList.remove("open");
  document.getElementById("overlay-bg").classList.remove("show");
  document.body.style.overflow = "";
  setTimeout(() => openModal(id), 100);
}

function openCheckout(){
  if(!cart.length) return;
  document.getElementById("coSuccess").style.display = "none";
  document.getElementById("coFormWrap").style.display = "";
  document.getElementById("checkoutModal").classList.add("open");
  document.getElementById("overlay-bg").classList.add("show");
}
function closeCheckout(){
  document.getElementById("checkoutModal").classList.remove("open");
  document.getElementById("overlay-bg").classList.remove("show");
  document.body.style.overflow = "";
}
function submitCheckout(){
  // Simple validation
  const name = document.getElementById("coName").value.trim();
  const phone = document.getElementById("coPhone").value.trim();
  const addr = document.getElementById("coAddr").value.trim();
  if(!name || !phone || !addr){
    alert("Please fill required fields");
    return;
  }
  document.getElementById("coFormWrap").style.display = "none";
  document.getElementById("coSuccess").style.display = "block";
  setTimeout(() => {
    cart = []; discount = null;
    renderCart();
    closeCheckout();
    notify("Commission request sent successfully");
  }, 1500);
}

function closeAll(){
  ["cart","modal","wishPanel","checkoutModal"].forEach(id => {
    document.getElementById(id).classList.remove("open");
  });
  document.getElementById("overlay-bg").classList.remove("show");
  document.body.style.overflow = "";
}

function notify(msg){
  const n = document.getElementById("notif");
  n.innerHTML = msg;
  n.classList.add("show");
  setTimeout(() => n.classList.remove("show"), 2600);
}

/* Journal */
function renderJournal(){
  document.getElementById("journalGrid").innerHTML = journals.map(j => `
    <article class="journal-card reveal">
      <img class="journal-img" src="\( {j.img}" alt=" \){j.title}">
      <div class="journal-body">
        <div class="journal-date">${j.date}</div>
        <div class="journal-entry-title">${j.title}</div>
        <div class="journal-excerpt">${j.excerpt}</div>
      </div>
    </article>`).join("");
}

/* Custom Form */
function previewImg(e){
  const f = e.target.files[0]; if(!f) return;
  const r = new FileReader();
  r.onload = ev => {
    const p = document.getElementById("uploadPrev"), t = document.getElementById("uploadTxt");
    p.src = ev.target.result; p.classList.add("shown"); t.textContent = f.name;
  };
  r.readAsDataURL(f);
}
function submitCustom(e){
  e.preventDefault();
  document.getElementById("customForm").style.display = "none";
  document.getElementById("customOk").style.display = "block";
}

/* Reveal Animation */
const observer = new IntersectionObserver(entries => {
  entries.forEach(e => {
    if(e.isIntersecting){ e.target.classList.add("visible"); observer.unobserve(e.target); }
  });
}, {threshold: 0.08});

document.querySelectorAll(".reveal").forEach(el => observer.observe(el));

/* Keyboard */
document.addEventListener("keydown", e => { if(e.key === "Escape") closeAll(); });

/* Init */
updateWishBadge();
renderGallery();
renderJournal();
renderCart();
</script>
</body>
</html>
